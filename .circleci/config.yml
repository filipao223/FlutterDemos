version: 2
jobs:
  build:
    docker:
      - image: cirrusci/flutter
    working_directory: ~/crypto_track
    steps:
      - checkout
      - run: echo $PWD
      - run: flutter doctor
      - run: flutter -v build apk
